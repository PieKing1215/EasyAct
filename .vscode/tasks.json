{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SCSS",
            "type": "shell",
            "command": "sass --watch scss:gen_css",
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}
